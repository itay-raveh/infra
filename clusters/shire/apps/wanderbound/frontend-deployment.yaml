apiVersion: apps/v1
kind: Deployment
metadata:
  name: wanderbound-frontend
  namespace: wanderbound
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: wanderbound-frontend
  template:
    metadata:
      labels:
        app: wanderbound-frontend
    spec:
      containers:
        - name: frontend
          image: ghcr.io/itay-raveh/wanderbound-frontend:latest@sha256:a0be3b4147c133e11f2808fc55ab27f7d3fa0c6b5af85be0eebe15876274198d # {"$imagepolicy": "flux-system:wanderbound-frontend"}
          ports:
            - containerPort: 8080
          env:
            - name: BACKEND_HOST
              value: "wanderbound-backend"
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "250m"
          startupProbe:
            httpGet:
              path: /
              port: 8080
            failureThreshold: 10
            periodSeconds: 2
          livenessProbe:
            httpGet:
              path: /
              port: 8080
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            periodSeconds: 5
          securityContext:
            runAsUser: 101
            runAsGroup: 101
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            readOnlyRootFilesystem: true
            seccompProfile:
              type: RuntimeDefault
          volumeMounts:
            - name: nginx-conf
              mountPath: /etc/nginx/conf.d
            - name: nginx-cache
              mountPath: /var/cache/nginx
            - name: nginx-run
              mountPath: /var/run
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: nginx-conf
          emptyDir: {}
        - name: nginx-cache
          emptyDir: {}
        - name: nginx-run
          emptyDir: {}
        - name: tmp
          emptyDir: {}
