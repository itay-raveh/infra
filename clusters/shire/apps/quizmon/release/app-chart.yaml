apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: quizmon
  namespace: quizmon
spec:
  interval: 5m
  url: https://github.com/itay-raveh/quizmon.git
  ref:
    branch: main
  ignore: |
    /*
    !/charts/
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: quizmon
  namespace: quizmon
spec:
  interval: 5m
  releaseName: online
  timeout: 10m
  chart:
    spec:
      chart: ./charts/quizmon
      sourceRef:
        kind: GitRepository
        name: quizmon
      reconcileStrategy: Revision
  install:
    remediation:
      retries: 0
  upgrade:
    remediation:
      retries: 0
  valuesFrom:
    - kind: ConfigMap
      name: quizmon-runtime
      valuesKey: values.yaml
  values:
    releaseImage:
      repository: ghcr.io/itay-raveh/quizmon/release
      digest: sha256:3bcedd3718d3b7bb9d32af1fd58123bab9bc4418b40b4103f9e381f2f0fdc4f3 # {"$imagepolicy": "flux-system:quizmon:digest"}
    runtimeConfig:
      version: 1
      workerName: quizmon
      origin: https://quizmon.raveh.dev
      sync:
        version: 1
        endpoint: https://quizmon-sync.raveh.dev
        audience: quizmon
      mailFrom: noreply@raveh.dev
      authRateLimitNamespace: "1011"
      apiRateLimitNamespace: "1012"
    powersync:
      enabled: true
